# BAM Local Cluster Example Configuration
# 
# This configuration sets up a local Solana cluster with 4 validators
# for BAM (Block Assembly Marketplace) testing.

# BAM service URL
bam_url = "http://127.0.0.1:45678"

# Tip manager configuration
tip_payment_program_id = "T1pyyaTNZsKv2WcRAB8oVnk93mLJw2XzjtVYqCsaHqt"
tip_distribution_program_id = "4R3gSG8BpU4t19KYj8CfnbtRpnT8gtk4dvTHxVRwc2r7"

# Faucet address for airdrops
faucet_address = "127.0.0.1:12345"

# Base directory for ledger storage
ledger_base_directory = "config"

# Build output directory (e.g., "target/debug" or "target/release")
validator_build_path = "./target/release"

# Validator configurations
# The first validator is automatically the bootstrap node
# Other validators will connect to the bootstrap node
[[validators]]
# Optional: Path to geyser plugin configuration
# geyser_config = "/path/to/geyser/config.json"

[[validators]]
# Second validator (optional)
# geyser_config = "/path/to/geyser/config.json"

[[validators]]
# Third validator (optional)
# geyser_config = "/path/to/geyser/config.json"

[[validators]]
# Fourth validator (optional)
# geyser_config = "/path/to/geyser/config.json"
